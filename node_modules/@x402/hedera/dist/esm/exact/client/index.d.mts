import { SchemeNetworkClient, PaymentRequirements, PaymentPayloadContext, PaymentPayloadResult } from '@x402/core/types';
import { C as ClientHederaSigner } from '../../signer-DCjZrivX.mjs';
import '@hiero-ledger/sdk';

/**
 * Hedera client implementation for the Exact payment scheme.
 */
declare class ExactHederaScheme implements SchemeNetworkClient {
    private readonly signer;
    readonly scheme = "exact";
    /**
     * Creates a new ExactHederaScheme instance.
     *
     * @param signer - Client-side Hedera signer
     */
    constructor(signer: ClientHederaSigner);
    /**
     * Creates a payment payload for exact Hedera payments.
     *
     * @param x402Version - x402 protocol version
     * @param paymentRequirements - Selected payment requirements
     * @param _ - Optional payment payload context
     * @returns Payload result
     */
    createPaymentPayload(x402Version: number, paymentRequirements: PaymentRequirements, _?: PaymentPayloadContext): Promise<PaymentPayloadResult>;
}

export { ExactHederaScheme };
