/**
 * @typedef {import("@hiero-ledger/proto").com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.BlockNodeApi} IBlockNodeApi
 */
/**
 * An enumeration of well-known block node endpoint APIs.
 */
declare class BlockNodeApi {
    /**
     * @internal
     * @param {number} code
     * @returns {BlockNodeApi}
     */
    static _fromCode(code: number): BlockNodeApi;
    /**
     * Parses an enum-name string as returned by the mirror node REST API
     * (e.g. `"OTHER"`, `"STATUS"`, `"SUBSCRIBE_STREAM"`). Punctuation and
     * casing are tolerated so `"BlockNode"`, `"block-node"`, etc. all
     * normalize to the same value.
     *
     * @internal
     * @param {string} name
     * @returns {BlockNodeApi}
     * @throws {Error} If `name` is `"UNRECOGNIZED"` or an unknown value.
     */
    static _fromString(name: string): BlockNodeApi;
    /**
     * @hideconstructor
     * @internal
     * @param {number} code
     */
    constructor(code: number);
    /** @readonly */
    readonly _code: number;
    /**
     * @returns {string}
     */
    toString(): string;
    /**
     * @returns {IBlockNodeApi}
     */
    valueOf(): IBlockNodeApi;
}
declare namespace BlockNodeApi {
    let Other: BlockNodeApi;
    let Status: BlockNodeApi;
    let Publish: BlockNodeApi;
    let SubscribeStream: BlockNodeApi;
    let StateProof: BlockNodeApi;
}
export default BlockNodeApi;
export type IBlockNodeApi = import("@hiero-ledger/proto").com.hedera.hapi.node.addressbook.RegisteredServiceEndpoint.BlockNodeEndpoint.BlockNodeApi;
